{% extends "layout/base.twig" %}

{% block title %}Cities - Demo{% endblock %}

{% block content %}
    <h1>Cities</h1>

    <p>Just a test controller with some dummy data.</p>

    <ul>
        {% for city in cities %}
            <li>{{ city.name }}, {{ city.state_code }}</li>
        {% endfor %}
    </ul>
{% endblock %}